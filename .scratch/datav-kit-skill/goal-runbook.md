# datav-kit-skill Goal Runbook

## Source Baseline

| Path | SHA-256 |
| --- | --- |
| `.scratch/datav-kit-skill/assets/r1-design-conventions.md` | `a1e18320c38f1684f907d1abcdee4a74539ac9e2e17b16d89b3820d9e78b5e7a` |
| `.scratch/datav-kit-skill/assets/r2-echarts.md` | `e7bd2662385652510d396cd3d60418c3cbe82a7fd9bf53c5e7e19f1417bfefaf` |
| `.scratch/datav-kit-skill/assets/r3-composition-patterns.md` | `4f9eb7354fab6f8b500599f1b64b6b12379d567bd078af512b7892ce9384fe8b` |
| `.scratch/datav-kit-skill/implementation-plan.md` | `d2dcb263242dc7b46b16e36a9bf5e269d484ff2383379b9642f45605203a7ee2` |
| `.scratch/datav-kit-skill/issues/01-design-conventions-baseline.md` | `67bd514364f34d85b57b4dc8bbf1fd41e6eb3861fc3557fa9fcc75bf61690f97` |
| `.scratch/datav-kit-skill/issues/02-echarts-theming.md` | `72e0aece49e6165cf430e276545cbc8ac600b59e184edc83bc77852fb757efc2` |
| `.scratch/datav-kit-skill/issues/03-composition-patterns.md` | `f8c7c61b7cabeba60cb245a077695bbb22eec5d6d9cd9f94a527cd04795990d0` |
| `.scratch/datav-kit-skill/issues/04-design-rules-and-rubric.md` | `afbaa8c1ba0ff25b2cb85289e3478f560679e248b1f71778c0d9e9f25dd6be84` |
| `.scratch/datav-kit-skill/issues/05-design-tokens.md` | `a73160516731b3355ee9c84d1be0f99a2db1e0996c2a36d3007dca0a962a10fe` |
| `.scratch/datav-kit-skill/issues/06-customization-boundary.md` | `f74468fb2399cb75522ae8db0c7973d66dfe8d00a1036d0c3348f2934093174f` |
| `.scratch/datav-kit-skill/issues/07-grid-and-degradation.md` | `d0ae3b4158607c4078aaef9199e2fe198bee85234f0308a498a618f952b483ab` |
| `.scratch/datav-kit-skill/issues/08-prototype-templates.md` | `453207e248407e255c10bb43228d0602d601362d9116736f66b53579bd3b14b8` |
| `.scratch/datav-kit-skill/issues/09-knowledge-base-routing.md` | `4538dd99d442f20fb7f24e4567f34b781be344226089d472bcf7416c6a2dee8f` |
| `.scratch/datav-kit-skill/issues/10-echarts-templates.md` | `a5316c9b066ec3f043199f8284c465c5f8bfca704a6bd98363b5841eb8110904` |
| `.scratch/datav-kit-skill/issues/11-workflow-checkpoints.md` | `16cf6313a784f0c5e9dfda7e0fffa52ca69766d808023487a3fc1cc68749dea6` |
| `.scratch/datav-kit-skill/issues/12-skill-file-tree.md` | `da7ba35e5bd715dba56400650be5a2a7b8e2ad63e403a0667fd027dc07440b2d` |
| `.scratch/datav-kit-skill/issues/13-spec-assembly.md` | `744a348b02b246f4fcb9e9be21e4d796646456fb31bc4c2495fe389902edf7d3` |
| `.scratch/datav-kit-skill/issues/14-title-bar-adjudication.md` | `8ebcb4c4fbc31a793fe4406e1683438eb5404b464e6dd36b0391dcba58f177a5` |
| `.scratch/datav-kit-skill/issues/15-project-theme-generation.md` | `f1654f9554d43f99e87b37125292ca62e0c041699e0a550e5986c6bfb7a6da56` |
| `.scratch/datav-kit-skill/issues/16-one-theme-per-screen.md` | `a448a9802cf3cb5dc8ff351e78cca2e03d15a47970ca254043c80d4ba6a1e9ae` |
| `.scratch/datav-kit-skill/issues/17-minimal-example.md` | `a83c94581d827b940067f4bb6f947afb4257237caa12ef7881e3b2d065926444` |
| `.scratch/datav-kit-skill/issues/18-border-box-selection.md` | `5ad28aed2caaea1d23270edbf4f01f9b3fddbcfe4c5997d35a6e481a9238508a` |
| `.scratch/datav-kit-skill/map.md` | `79de62641300f24d44d32cb4707007fde710bfc6345fd811e49a108b4ec6adc2` |
| `.scratch/datav-kit-skill/spec.md` | `406f867a6482cdf5f76d4ffb94bf8535c2ee807a1e5c656d670198c3e49b8756` |
| `CLAUDE.md` | `694a2d721e41c385f3db492838c23299826df5ba9809e3b0721aac70021e196a` |
| `docs/components/borders/border-box-1.md` | `0be1ce5ff1f9276ba619ac8fd55c350525023ac5d13ccfae85122c4f55dfde08` |
| `docs/components/borders/border-box-10.md` | `e4efbede791c4e6545df005d1ef8ccd6cd4d95400ad75372ecfb7d1701bcb58f` |
| `docs/components/borders/border-box-11.md` | `cd23eb3e3748bc04ac1e58ea609f7678fae97b9ab5381e155dc238569d4fa18d` |
| `docs/components/borders/border-box-12.md` | `0cac037a9e24773325f90ecd46614ec4e7029b4488d42d85eca508db969b7dad` |
| `docs/components/borders/border-box-13.md` | `532cae2acd6b30edb7cf84eecbf501387199743e59a0f5ee8d8965ad7a98c4ef` |
| `docs/components/borders/border-box-14.md` | `2318e2af5f21c4b3393bf9f77fb9d26d4aeff07a0b05bb5fd0cc6bc09ff1c65e` |
| `docs/components/borders/border-box-15.md` | `f6dde52f4cac4c7edb9ddfbbaa30d7430bc3a035a2ad296ee5b2898edb00422a` |
| `docs/components/borders/border-box-16.md` | `d3bc35402bf7f6ec91c1b96fc6256563cadf0e015773f46538347992dd4ebd95` |
| `docs/components/borders/border-box-2.md` | `b8dcaa2a67ba753e57e942385e145f6324d7000d2af2fb2b5c40b8f31dc18b96` |
| `docs/components/borders/border-box-3.md` | `5c415fed851cc6aba42b9753851136d856a0f1732182905e1e52c32448897e66` |
| `docs/components/borders/border-box-4.md` | `32694d2da2a30115e8d92c2e55b6eb5f9cd2d7eee89d84d990d4a4318dc8b38d` |
| `docs/components/borders/border-box-5.md` | `5224d3aeb75817c6dfd34f13c497cb54bb623555d0d5246e258cb7e7c5584688` |
| `docs/components/borders/border-box-6.md` | `82563191affd0b92004cbc813d7ada6520b107fa4731f9d101f7696a71d82c4a` |
| `docs/components/borders/border-box-7.md` | `64d8e8bb2947658aab7e9dbbfff2301531981a53cc6e4bf91c1818ea771b2c31` |
| `docs/components/borders/border-box-8.md` | `ee3731a19c2999c6566dd5c6709cad2c1f3cee2a159e539b3dfe0f84a22d2d42` |
| `docs/components/borders/border-box-9.md` | `54de7ecd5e888de1e991fa3ce9e1e9207ce461a13c92a8d45f3a9c5fa885f135` |
| `docs/reference/architecture-contracts.md` | `64a5098e34d836e14ca154d2c70266cddee2b3abe126701458a6c4f711781f8d` |
| `eslint.config.js` | `881706fd7810b1f8f5edac11fc1ab0c0d4a4bda5c5d15c30b8e47ab62096d618` |
| `package.json` | `ab1600626396542fcf5efe56f2b7be405ece1c23d6bb5a0c2a0bb17d2b8bca37` |
| `pnpm-lock.yaml` | `f11ecceeb8c52a3884c8ceab1c89689c12a771556180304ff0d7df9343669454` |
| `pnpm-workspace.yaml` | `405014c69c72e7d3b6d9ba84eb02a6be958ba965d4133ab735fd24dbea293f81` |
| `tsconfig.json` | `cf361e261ed469897157e00ad7a861e70b891795f6725ae0c4a792531f8aeb3b` |
| `turbo.json` | `a1dd3ea0d16f5cb14df397b6e9e4384ffad431266bb4274b43c761a376fbd6bb` |

## State Rules

- `implementation-plan.md` 是 Gate 合同的唯一来源；本账本只记录状态和证据。
- 一次只执行 Goal Ledger 中唯一 `active` 的 Gate。
- 每轮向对应 Gate 的 Progress Log 追加 slice、修改、验证结果、风险和下一动作。
- `passed` 需要计划中每条 Exit condition 的明确证据；普通实现或验证失败保持 `active`。
- `blocked` 只用于计划声明的 Stop condition，并记录阻塞与恢复条件。
- 人工验收待确认是一次 Goal 终止交接，必须记录在 Progress Log；它不是 `blocked`，当前 Gate 必须保持 `active`，不得通过重复日志表示等待。
- 当前 Gate 通过后只激活直接后继并结束本次 Goal；直接后继虽为 `active`，但必须由新的 Goal 执行。最后一个 Gate 通过后记录 effort 完成并结束本次 Goal。

## Goal Ledger

| Gate | Status | Depends on | Plan contract | Unlock evidence |
| --- | --- | --- | --- | --- |
| G0: Token and Design-Rule Foundation | passed | none | `implementation-plan.md` → `G0: Token and Design-Rule Foundation` | no predecessor; all Exit conditions met 2026-09-09 |
| G1: Knowledge-Base Routing | passed | G0 | `implementation-plan.md` → `G1: Knowledge-Base Routing` | G0 passed 2026-09-09; all Exit conditions met 2026-09-09 |
| G2: Skill Entrypoint | passed | G1 | `implementation-plan.md` → `G2: Skill Entrypoint` | G1 passed 2026-09-09; all Exit conditions met 2026-09-09 |
| G3: Prototype Skeletons | passed | G2 | `implementation-plan.md` → `G3: Prototype Skeletons` | G2 passed 2026-09-09; all Exit conditions met 2026-09-09 |
| G4: Chart Guidance and Templates | passed | G3 | `implementation-plan.md` → `G4: Chart Guidance and Templates` | G3 passed 2026-09-09; all Exit conditions met 2026-09-09 |
| G5: Project Theme and Contrast Utility | passed | G4 | `implementation-plan.md` → `G5: Project Theme and Contrast Utility` | G4 passed 2026-09-09; all Exit conditions met 2026-09-09 |
| G6: Minimal Example and Release Acceptance | passed | G5 | `implementation-plan.md` → `G6: Minimal Example and Release Acceptance` | G5 passed 2026-09-09; all Exit conditions met 2026-09-09 — Leo replied `通过`; effort complete (no successor) |

## Progress Log

### G0: Token and Design-Rule Foundation

- 2026-09-08: initialized as `active`; implementation has not started.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/assets/tokens.css` (78 lines). 37 `--dvk-screen-*` tokens across five groups (spacing 9 / typography 12 / layering 4 / motion 6 / layout 6), declared only on `.dvk-screen`, no `:root`, no color declaration or color literal. Directed check: every token name and default value compared against `spec.md` §5 → all groups PASS (spacing 4/8/12/16/24/32/48/64/96; type 14/18/24/32/44 + hero 56; weight 400/500/600; line-height 1.25/1.5; z 0/10/100/1000; duration 150/250/300ms + three ease curves; layout 48/12/24/104/104/24). Variance noted, no Stop condition: issue 04 §2.1 lists `40` inside the 8pt scale, while issue 05 (the token-set decision) and `spec.md` §5 omit it; the token authority is issue 05 + spec §5, so 40 remains a legal 8pt multiple but is not a token.
- 2026-09-09: slice 2 — wrote `skills/datav-kit/references/tokens.md` (130 lines). Directed parity: 37/37 token names and values match `tokens.css` (no missing, no extra, no value mismatch); scope stated as `.dvk-screen` / never `:root`; precedence stated as `project override > skill default > component fallback`; calibration contract `(viewing distance / 200) x (1080 / screen height)` plus raise-the-whole-scale and monitor/laptop rules; motion coexistence documented (interaction 150–300ms via `--dvk-screen-duration-*`, decoration keeps theme `--dvk-motion-duration` 2200–2600ms, charts 200–400ms); hero 56 marked as a metric exception, not a sixth text level. Color-declaration scan over both token files: no `--dvk-color-*` declaration, no color literal.
- 2026-09-09: slice 3 — wrote `skills/datav-kit/references/design-rules.md` (144 lines): six groups, 41 entries (9 `[redline]` / 32 `[advisory]`), 8 🅰 accessibility markers, all seven source labels in use, no-rounding contrast rule (`4.499:1 is not 4.5:1`), one-theme-per-screen rule, and rubric derivation (1:1, binary redline, three-level advisory, no total score, writeback). Colors referenced only as `--dvk-color-*`; no color literal or second color source.
- 2026-09-09: repository verification — `pnpm lint` exit 0 (eslint processed `references/design-rules.md` and `references/tokens.md`, 0 messages; `assets/tokens.css` falls outside the antfu lint scope and is covered by the directed checks above); `git diff --check` exit 0 with empty output, run via `git add -N` on the three new files followed by `git reset` to restore the untracked working-tree state. Source Baseline: all 48 listed SHA-256 inputs match, so execution used the locked baseline. Language basis: `map.md` 已定决策 “正文英文；业务文案中文；另附中文 README.md”, so the three foundation files are written in English.
- 2026-09-09: Exit conditions — (1) PASS: `assets/tokens.css` holds the complete five-group `--dvk-screen-*` reference implementation under `.dvk-screen` (37 tokens, single selector). (2) PASS: `references/tokens.md` and `assets/tokens.css` agree on names, values, scope, precedence, and calibration formula. (3) PASS: `references/design-rules.md` holds the six groups, accessibility cross-rules, thresholds with source labels, and rubric derivation, with no second color source. (4) PASS: directed checks clean, `pnpm lint` exit 0, `git diff --check` exit 0. Manual acceptance: 无. Risks: none open; only carry-forward note is that lint does not cover CSS, so `assets/tokens.css` relies on the directed value/scope/color checks in later Gates.
- 2026-09-09: G0 `active` → `passed`; G1 `planned` → `active` — 已激活，尚未开始实施. Effort continues in a new Goal; this Goal ends here.

### G1: Knowledge-Base Routing

- 2026-09-08: initialized as `planned`; no implementation evidence.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/references/patterns.md` (492 lines): routing path (brief → template → pattern → component, no "task → component" table), T1–T4 template layer, a P1–P19 route index, the 19 pattern entries, the normative P6 matrix, and the cross-pattern rules. Directed: P1–P19 present with all six labelled fields (Purpose / Construction / Parameters / Grid position / Replacements / Cautions) → 19/19 PASS; P6 role entries and fixed fallback chains compared row-by-row against the locked snapshot in `spec.md` §3.4 and against issue 18 → 16/16 rows match on preferred variant and on chain order (no missing, no extra, no drift); capability-group variant sets match issue 18 → PASS; `title-4` appears only in two prohibitions and is never usable → PASS; color scan → no color literal and no second color source, `--dvk-color-*` references only.
- 2026-09-09: slice 2 — wrote `skills/datav-kit/references/components.md` (169 lines): publication status list, the `customElements.get(tag)` runtime rule, the 16-row border-box capability table, the shared `contentRect` content-area contract, the online-first fetch judgement, and the `main`-branch fallback with its mandatory label. Directed: all 16 capability rows verified field-by-field against the local `docs/components/borders/border-box-*.md` prop tables → `background-color` (7/8/9/10/15 only), `animated`/`paused` (1/8/10/11/12/13/14/16), `auto-height` (1 only), `glow-intensity` default (1 on 2–6 and 11–14, 0.7 on 16), content-area source (`contentRect` on all 16), publication (1–15 published, 16 main-only) → all PASS; statuses 30 published / 5 main-only / `title-4` nonexistent present; online index, detail URL, and raw `main` fallback documented with the "may not be published" label; P6 role matrix deliberately not duplicated (pointer to `patterns.md` §3) → PASS.
- 2026-09-09: repository verification — `pnpm lint` exit 0 (one lint finding from this Gate: two `style/no-multi-spaces` errors in the `components.md` JS block, fixed by single-spacing the inline comments; re-run clean); `git diff --check` exit 0 with empty output, run via `git add -N` on the two new files followed by `git reset` to restore the untracked working-tree state. Source Baseline: every G1 input matches its locked SHA-256 — `spec.md`, `implementation-plan.md`, issues 03/09/14/18, `assets/r3-composition-patterns.md`, `docs/reference/architecture-contracts.md`, `CLAUDE.md`, and all 16 border-box docs (25/25). Working tree holds only the two new reference files; no unrelated file was touched.
- 2026-09-09: Exit conditions — (1) PASS: the 19-pattern route table is complete and routes brief → template → pattern → component. (2) PASS: P6 carries the locked role entries, fixed fallback chains, surface/HUD boundary, motion rule, `contentRect` rule, and default-slot title rule, matching `spec.md` §3.4 and issue 18 row-for-row. (3) PASS: the component table carries the runtime availability method, published/main-only/nonexistent statuses, the six capability fields, and the documented online fallback. (4) PASS: directed checks clean, `pnpm lint` exit 0, `git diff --check` exit 0. Manual acceptance: 无. Risks / carry-forward: (a) the online index host `hackycy.github.io` was unreachable from this environment, so the index-side 404 trigger is documented from the resolved decisions (issues 09/14) while only the raw `main` fallback URL was live-verified (HTTP 200 for `.../docs/components/titles/title-1.md`); a later Gate with network access should re-probe the index. (b) Pattern-layer normalizations made inside this Gate's remit (r3 §8 items left open, no resolved ticket changed, P6 matrix untouched): P3 standard track = `dvk-decoration-9` (`-6` accepted alternative), P4 recommended KPI card frame = `dvk-border-box-15`, P7 normative head = `p` + `h3` with optional right zone, P9/P12 bar mechanism = `--bar-value`, P14 hero size = the token 56px superseding the demo's 52px, P16 one count-up duration per screen.
- 2026-09-09: G1 `active` → `passed`; G2 `planned` → `active` — 已激活，尚未开始实施. Effort continues in a new Goal; this Goal ends here.

### G2: Skill Entrypoint

- 2026-09-08: initialized as `planned`; no implementation evidence.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/SKILL.md` front matter plus §1 applicability and §2 coverage/preflight (64 lines at slice close). Directed: `description` compared character-for-character against `spec.md` §3.1 → **exact match**, not merely semantic equivalence; no `disable-model-invocation` key; applicability boundary states both the use cases (数据大屏 / dashboard wall / control-room screen, element selection, layout, review) and the exclusions (ordinary back-office pages, non-datav-kit projects); runtime preflight present with `customElements.get(tag)` used twice — the registration example and the `dvk-title-1` header decision — plus the 30 published / 5 main-only / `title-4` nonexistent summary, the online-first detail protocol with its labelled `main` fallback, the one-theme-per-screen rule, and `--dvk-color-*` as the only color source → PASS.
- 2026-09-09: slice 2 — appended §3 six-step workflow, §4 five hard gates, §5 seven brief questions, §6 redline quick reference, §7 reference index (file now 145 lines). Directed: workflow rows 6/6, each carrying artifact + exit condition, matched row-for-row against `spec.md` §4.1 (`design/brief.md`; `design/prototype-*.html` 2–3, double-click with no console errors; `design/decision.md`, explicit choice + deviation registration; project code running at target resolution; `design/self-check.md`, every redline passes; `design/review.md`, record + skill revision items); hard gates 5/5 matched against `spec.md` §4.2 and issue 11 §2 (no implementation before selection, no prototype before brief, no review before redlines pass, skeleton or redline change → back to prototype, unregistered advisory deviation = defect); brief questions 7/7 matched against `spec.md` §4.3; redline classes 4/4 = Readability / Geometry / Semantics / Accessibility, each reduced to its headline redline with ownership explicitly handed to `references/design-rules.md`; reference index splits the five resident G0/G1 artifacts from the on-demand entries. Cross-link resolution: `references/design-rules.md`, `references/patterns.md`, `references/components.md`, `references/tokens.md`, `assets/tokens.css` → 5/5 EXIST; the six remaining on-demand entries are the locked `spec.md` §2 files scheduled for G3–G6 → PASS.
- 2026-09-09: repository verification — `wc -l skills/datav-kit/SKILL.md` = 145, at most the 250-line limit (105 lines of headroom); `pnpm lint` exit 0, and `npx eslint --no-cache skills/datav-kit/SKILL.md` confirms the file is actually inside the antfu markdown scope at 0 errors / 0 warnings (not merely skipped); `git diff --check` exit 0 with empty output, run via `git add -N` followed by `git reset` to restore the untracked working-tree state. Source Baseline: all 48 locked SHA-256 inputs match, so execution used the locked baseline. Working tree holds only the new `SKILL.md`; no unrelated file was touched. Language basis: body guidance in English with Chinese limited to business copy (`大屏`, `数据大屏`), per `map.md` 已定决策.
- 2026-09-09: Exit conditions — (1) PASS: 145 ≤ 250; `description` is the locked `spec.md` §3.1 text verbatim; §1 carries the applicability boundary. (2) PASS: six workflow steps with artifacts/exit conditions, five hard gates, seven brief questions, four redline classes, and the `customElements.get(tag)` runtime preflight are all present (counts 6 / 5 / 7 / 4 / 2). (3) PASS: the five resident progressive-disclosure links resolve to the G0/G1 artifacts, and the file defers ownership to them instead of restating their contracts. (4) PASS: directed checks clean, `pnpm lint` exit 0, `git diff --check` exit 0. Manual acceptance: 无. Risks / carry-forward: (a) the six on-demand index entries point at `spec.md` §2 files that G3–G6 have not created yet, so those links resolve only after later Gates; the index is complete by contract. (b) §6 repeats the headline thresholds (4.5:1 / 3:1 / distance ÷ 200) as an index-level summary while naming `references/design-rules.md` as the owner — the same relationship `spec.md` §3.4 has to P6. (c) No second color source: no color literal anywhere, and `--dvk-color-*` / `.dvk-theme-*` are referenced only.
- 2026-09-09: G2 `active` → `passed`; G3 `planned` → `active` — 已激活，尚未开始实施. Effort continues in a new Goal; this Goal ends here.

### G3: Prototype Skeletons

- 2026-09-08: initialized as `planned`; no implementation evidence.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/assets/prototypes/t1-three-column.html` (T1 三栏运维). Declares the four required sections as labelled comment blocks (1/4 skeleton, 2/4 block list with P refs, 3/4 component construction, 4/4 placeholder-data shape) plus a permission-tiers block; loads `../tokens.css`, maps `@datav-kit/elements@0.0.5` to jsDelivr `+esm` with the esm.sh backup documented, and drives every block from inline JSON in `#dvk-data`. Blocks match the locked T1 row (spec §3.10 / issue 08 §1): header P2+P3, KPI P4 x4, left P6+P7+P9 / P6+P7+P10, centre P6+P7+P17 (P18 markers + P19 overlay) + P12, right P6+P7+P11 + P6+P7+P13 (two of the allowed `P11 / P15 / P13 x2-3`). Directed: 17/17 static checks PASS (four declarations present; P refs 15 distinct; tokens.css linked; import map + backup; `customElements.get` feature detection over dvk-title-1/-2/-3 only; no `title-4` token; exactly one theme class; fit-screen 1920x1080 contain centre; 8 `minmax(0, 1fr)` rows; no colour literal in the stylesheet; inline JSON parses). Browser smoke (`file://`, Chrome 152 headless via Playwright, 1920x1080): 8/8 probed elements registered, **0 console errors / 0 warnings / 0 page errors / 0 failed requests**, title fallback path renders (2 x `dvk-decoration-9` paired rails + centred `h1`), 9 `dvk-count-to`, one `.dvk-theme-*` class, theme switch cyber-blue → matrix-green flips `--dvk-color-primary` #18f0ff → #32ff7e and keeps the page ground in sync, no element overflows its box (`.panel-inner` / `.panel-body` / `.map-container` / columns / screen all scrollHeight == clientHeight). Chart slot: none in T1 — the locked block list gives T2 the only chart slots (`主区：P6 + 图表槽 ×2（上下）`), and P9/P10/P12/P13 are DOM visualisations. Risks / deviations: (a) registered deviation per components.md §4 — `--dvk-border-box-4-padding` is overridden on the P17 panel (96/32/24) because border-box-4's top-left ornament intrudes past the automatic contentRect inset (41.87/36.55) at 900x464 and hid the panel head; recorded in the file's comment and here. (b) `--dvk-screen-space-*`-based paddings stay token-derived; no colour literal was introduced. (c) The P10 name column is `minmax(0, 150px)` instead of the pattern's 120px so `SH-01 浦东枢纽` does not ellipsise — a block-proportion (recommended-value) scene adjustment. (d) A 1px sub-pixel rounding gap in the P12 body (163/164) is clipped by `overflow: hidden` and is not content clipping. Next: slice 2 — T2 两栏分析 with its two ECharts chart slots.

- 2026-09-09: slice 2 — wrote `skills/datav-kit/assets/prototypes/t2-two-column.html` (T2 两栏分析). Same four-section declaration format; blocks match the locked T2 row: header P2+P3, KPI P4 x4, main span 8 = two stacked P6+P7 panels each holding a chart slot (`主区：P6 + 图表槽 ×2（上下）`), side span 4 = P6+P7+P10 + P6+P7+P15. Both chart slots carry a minimal real ECharts instance: `echarts@6.1.0` ESM via import map, `init(el, themeObject, { renderer: 'svg' })` (07: SVG preferred), theme object built from `--dvk-color-*` / `--dvk-line-width` / `--dvk-screen-font-size-*` read off the nearest `.dvk-theme-*` host, `withAlpha()` for translucent roles, explicit `grid` override (never the 10% / 60 default), `animationDuration: 300` (chart motion 200–400ms, never `--dvk-motion-duration`), `ResizeObserver` + rAF-throttled `resize()`, and `chart.setTheme(obj)` on theme switch (never dispose + re-init). Directed: 17/17 static checks PASS (same set as T1). Browser smoke (`file://`, Chrome 152): both chart slots render real SVG (891x199 in a 1440x900 viewport, 1188x266 at 1920x1080), **0 console errors / 0 warnings / 0 page errors**, title fallback renders, 6 `dvk-count-to`, one theme class, no overflow, and theme switch cyber-blue → matrix-green/solar-gold flips both the DOM tokens and the chart series colours (`setTheme` proven: trend strokes rgb(24,240,255)/rgb(43,124,255) → rgb(255,209,102)/rgb(255,143,61), both charts still alive). Defect found and fixed in this slice: the inline module script could run before the CDN theme stylesheets were applied, so `getComputedStyle` token reads returned empty strings and the charts initialised unthemed (axes only, no series). Fixed by gating the themed boot (`applyTheme` + `mountChart`) behind `load` (with a `readyState === 'complete'` fast path); the same gate was applied to T1 for the page-ground sync. Risk / carry-forward: (a) one ECharts-internal helper `<path>` (138x24, positioned at -5,-5, outside the plot area) keeps the library's default `#b7b9be` stroke; it is not a rendered visual and no theme token can reach it — noted for G4's chart layer. (b) The T2 side column's P10 name column uses the same `minmax(0, 150px)` scene value as T1. Next: slice 3 — T3 单栏叙事.

- 2026-09-09: slice 3 — wrote `skills/datav-kit/assets/prototypes/t3-single-column.html` (T3 单栏叙事). Same four-section declaration format; blocks match the locked T3 row: header P2+P3 (no KPI row in this skeleton, so no P4), primary view P6+P7+P17 full-width (P18 markers x4 + P19 overlay card), then `3 个并排小面板` = P6+P7+P9 / P6+P7+P14 (hero, one per screen) / P6+P7+P16, and the 104px footer strip = P12 rhythm bars x12. Directed: 17/17 static checks PASS. Browser smoke (`file://`, Chrome 152, 1920x1080): **0 console errors / 0 warnings / 0 page errors**, title fallback renders (2 x dvk-decoration-9 paired rails + centred h1), 5 `dvk-count-to`, one theme class, no overflow on any box, theme switch flips the DOM tokens and the page ground. Defects found and fixed in this slice: (a) `--dvk-count-to-affix-font-size: 0.42em` resolved against the host's inherited 14px rather than the count-to font size, so the hero affix rendered ~6px (invisible); re-expressed as `calc(var(--dvk-screen-font-size-hero) * 0.42)`, and the same latent bug was fixed in T1's P13 ring (`calc(--dvk-screen-font-size-sm * 0.58)`) and P9 rows (`calc(--dvk-screen-font-size-sm * 0.75)`). (b) The 104px footer strip could not host the pattern's 72px bar slot plus its time/label rows: the top `time` is dropped (the bottom label already carries the hour) and the bar slot min-height is 56px in this variant. (c) `--dvk-border-box-13-padding` is overridden to 4/24/12 on the strip so the carrier-spine frame's bottom ornament does not cut the labels — registered deviation per components.md §4, commented in the file. Risk / carry-forward: (a) the T3 map panel carries the same `--dvk-border-box-4-padding` 96/32/24 override as T1 (same ornament obstruction at 1824x484). (b) No chart slot in T3, by the same reading as T1. Next: slice 4 — T4 KPI 主导.

- 2026-09-09: slice 4 — wrote `skills/datav-kit/assets/prototypes/t4-kpi-led.html` (T4 KPI 主导). Same four-section declaration format; blocks match the locked T4 row: header P2+P3 (compact title, no rails — P3's documented T4 replacement), KPI row at 2x `--dvk-screen-kpi-height` (208px) holding `P4 放大 KPI 卡 ×6` built from P14's construction + P16 count-to, and main 6/6 = P6+P7+P9 / P6+P7+P10. Directed: 17/17 static checks PASS. Browser smoke (`file://`, Chrome 152, 1920x1080): **0 console errors / 0 warnings / 0 page errors / 0 failed requests**, 14 `dvk-count-to`, title fallback path `p3-fallback-compact` with 0 rails, one theme class, no overflow, theme switch clean. Decision recorded: the six enlarged cards carry their value at `--dvk-screen-font-size-xl` (44px) rather than the hero 56px, because P14's caution reserves the hero size as a one-per-screen metric exception and T3 owns it; the cards still use P14's construction (surface panel + label + value + caption) and P16. Next: repository verification.

- 2026-09-09: repository verification — `pnpm lint` exit 0, **but the four prototype files are outside the antfu lint scope**: `npx eslint --no-cache skills/datav-kit/assets/prototypes/t1-three-column.html` reports "File ignored because no matching configuration was supplied" (0 errors / 1 warning, exit 0), and the same holds for the directory. The prototypes are therefore proven by the directed static + browser checks, not by lint; lint's exit 0 confirms this Gate introduced no lint regression anywhere else. `git diff --check` exit 0 with empty output, run via `git add -N` on the new directory followed by `git reset` to restore the untracked working-tree state. Source Baseline: all 48 listed SHA-256 inputs re-verified byte-for-byte → 48/48 match, so execution used the locked baseline and no locked input was modified. Working tree holds only the four new prototype files (plus this runbook's Progress Log); no unrelated file was touched.

- 2026-09-09: aggregate directed evidence (one run across all four files, 1920x1080, Chrome 152): skeleton geometry matches the locked table in every file — T1 rows 104/104/728 with main columns 438/900/438 (3:6:3), T2 rows 104/104/728 with 1208/592 (8:4), T3 rows 104/728/104 with the primary view 1824x484 and three equal 592px small panels, T4 rows 104/208/624 with 900/900 (6:6). Registration succeeds in all four (`dvk-fit-screen`, `dvk-count-to`, `dvk-decoration-9` plus the per-file border-box variants and `dvk-decoration-8` in T1). Chart slots: T2's two slots both render real ECharts SVG (1188x266), above the 160x100 minimum; T1/T3/T4 declare no chart slot because their locked block lists contain none — no empty slot exists anywhere. Title fallback: T1/T2/T3 render the P3 paired `dvk-decoration-9` rails + centred `h1`, T4 renders the compact block with no rails; the component path stays in the code and would take over the moment a `dvk-title-*` registers. One theme per screen in every file (exactly one `.dvk-theme-*` class), switching through all five library themes flips `--dvk-color-primary` and the page ground in each file, and T2's charts survive all five switches via `setTheme`. No `dvk-title-4` element or token anywhere; `[slot]` attribute count is 0 across all four files, so every panel head lives in the default slot. Zero console output of any kind across the four files.

- 2026-09-09: Exit conditions — (1) PASS: all four files exist at `assets/prototypes/t{1,2,3,4}-*.html` with the locked skeleton rows/columns, the locked block lists, real datav-kit components, and inline-JSON placeholder shapes declared in section 4/4 of each file. (2) PASS: each file opens from `file://` with 0 console errors, renders its real components (and T2's two chart slots), and supports theme switching plus the `customElements.get` title fallback. (3) PASS: geometry (104/104/1fr, 104/208/1fr, 104/1fr/104; 3/6/3, 8/4, 6/6), default-slot (no `slot` attribute, headings inside the default slot), one-theme-per-screen, and no-`title-4` all verified. (4) PASS: directed browser evidence + static checks + `pnpm lint` exit 0 + `git diff --check` exit 0; lint does not cover `.html`, which is recorded above rather than glossed. Manual acceptance: 无. Risks / carry-forward: (a) `--dvk-border-box-4-padding` (96/32/24) is overridden on the T1/T3 map panels because border-box-4's top-left ornament intrudes past the automatic contentRect inset at those sizes — a registered content-area deviation, commented in both files; G4's chart layer should not copy the override blindly. (b) `--dvk-border-box-13-padding` (4/24/12) and a 56px P12 bar-slot floor are used in T3's 104px footer strip for the same reason. (c) P10's name column is 150px (pattern value 120px) in T1/T2/T4 so `SH-01 浦东枢纽` does not ellipsise. (d) The `.panel--map` and strip deviations mean a future `references/patterns.md` revision may want to state that ornate HUD frames need a larger inset than `contentRect` at small aspect ratios. (e) One ECharts-internal helper path keeps the library's default grey; it is not rendered.
- 2026-09-09: G3 `active` → `passed`; G4 `planned` → `active` — 已激活，尚未开始实施. Effort continues in a new Goal; this Goal ends here.

- 2026-09-09 (post-G3 correction, user-reported): the three border-box padding overrides recorded in this Gate's slices were re-examined against the user's rule that the computed `contentRect` inset **is** the safe distance, differs per variant and per panel size, and must not be replaced by a fixed padding to gain room. Measured evidence at the real panel sizes (Chrome 152 headless, `file://`): T1 map panel 900x464 → default inset 41.9 top / 36.5 left, and a 4x crop shows the head clear of the border-box-4 ornament, so the `--dvk-border-box-4-padding` override was **removed as unnecessary**; T3 map panel 1824x484 → default inset 43.7 top / **74.1 left**, and the override had been setting left/right to 32px, i.e. moving content 42px *closer* to the frame than the safe area while buying vertical room, so the override was removed and replaced by a head-only offset (`.panel--map .panel-heading { padding-top: var(--dvk-screen-space-3xl) }`), which keeps the safe inset and clears the ornament that crossed the `SITUATION` eyebrow; T3 footer strip 1824x104 → default inset 16px gives a 72px content box that hosts the strip's own 56px `.rhythm__slot` min-height and its label with no clipping, so the `--dvk-border-box-13-padding` override was **removed**. Verification after the change: all four prototypes load from `file://` with 0 console errors / 0 warnings / 0 page errors and no `scrollHeight`/`scrollWidth` overflow on any box (`.screen`, `.screen-main`, `.panel-inner`, `.panel-body`, `.map-container`, `.map-shell`, `.rhythm-strip`, `.kpi-strip`); T1 head sits at the computed inset 41.9/36.5, T3 head at 43.7/74.1 with height 50 + 48 offset; `pnpm lint` exit 0; `git diff --check` exit 0. Guidance updated so the same mistake is not repeated: `references/components.md` §4 now states that the computed inset is the safe distance and must not be substituted to gain room, and that a decoration crossing one content row is fixed by moving that row, not the inset; `references/charts.md` §5 now scopes the token override to the chart's own breathing room and calls it a registered deviation for a measured problem. Files touched: `assets/prototypes/t1-three-column.html`, `assets/prototypes/t3-single-column.html`, `references/components.md`, `references/charts.md`. The prototype deviations recorded earlier in this log are superseded by this entry.

- 2026-09-09 (post-G3 correction 2, user-reported): the content inside the panels still sat too close to the frames. Rule confirmed by the user: the component's computed inset is the safe distance and is not modified; where content needs more room, an **inner wrapper carries the padding** — `.panel-inner`, `.kpi-card`, a marker's label plate. Fixes: (a) KPI cards in T1/T2/T4 (`dvk-border-box-15` / `-10` insets bottom out at 10-15px) and the panels at those insets (T1 `-7`/`-9`/`-15`, T2 `-9`/`-15`, T3 `-9`/`-10`/`-15`, T4 `-7`/`-9`/`-10`) now carry `padding: 0 var(--dvk-screen-space-lg)` on the inner wrapper — horizontal only, because measurement showed those fixed-height panels have no vertical slack (the KPI cards' `dvk-count-to` line box already exceeds its box by 3px, and an 8px vertical padding pushed `.kpi-strip` to 110px in a 104px row and `.small-row` to 226px in a 220px row; both reverted to horizontal-only, both clean). (b) T1's map head, which had been left at the default inset, now gets the same head-row nudge as T3 (`padding-top: var(--dvk-screen-space-lg)` — the ornament only crowds the first line there, unlike T3's 48px). (c) P18 markers: `strong` code + `span` name are wrapped in `.marker__label` with a `--dvk-color-surface` backing and `--dvk-screen-space-xs` inline padding, because the map's connection lines crossed `徐汇` / `临港`; applied in T1 and T3. (d) T1's P13 ring gauge (同步率) is now a centred flex group instead of a left-aligned two-column grid, so the gauge and its caption sit centred in the panel. Verification: all four prototypes load from `file://` with 0 console errors / 0 warnings / 0 page errors, before and after a theme switch to matrix-green, with no box-level `scrollHeight` overflow (the only remaining entries are pre-existing 2-7px text line-box rounding on `span.label`, `h1`, `dvk-count-to` — unchanged by this correction); the marker plate and KPI padding were re-checked under the light `ice-white` theme; `pnpm lint` exit 0; `git diff --check` exit 0. Guidance updated to match: `references/components.md` §4 now states the inner-wrapper rule and the horizontal-padding preference; `references/patterns.md` P4 records the KPI card's inner padding and P18 records the label plate. Files touched: `t1-three-column.html`, `t2-two-column.html`, `t3-single-column.html`, `t4-kpi-led.html`, `references/components.md`, `references/patterns.md`.

- 2026-09-09 (post-G3 correction 3, user-reported): the P17 focal primary view in T1 and T3 was swapped from `dvk-border-box-4` to `dvk-border-box-1`. Reason given: the prototype's focal frame should be simpler and not overly cyberpunk. Candidate comparison rendered side by side at the same size (border-box-1..6 with identical content): `-2`/`-5`/`-6` are busier than `-4`; `-3` is restrained but its top module crosses the panel head at this aspect (same collision class that needed the head nudge); `-1` is the only published frame with no ornament, so the head sits clear without any nudge and the map keeps the full panel. `-1` is transparent like `-4`, so the panel's relationship to the screen ambience is unchanged, and it is a sanctioned member of the role's fixed chain (`4 → 2 → 6 → 3 → 5 → 1`) — a registered deviation from the preferred `4`, documented in both files' declaration blocks. Follow-through: the `.panel--map .panel-heading` nudges were deleted from both files (nothing to clear any more), `dvk-border-box-1 .panel-inner` joined the inner-padding rule (8px computed inset + 16px inner = 24px), and the declaration comments now name `-1` with the deviation reason. Verification: all four prototypes from `file://` with 0 console errors / 0 warnings / 0 page errors and no box-level overflow; T1 and T3 re-screenshotted — head clear, map area unchanged or larger; `pnpm lint` exit 0; `git diff --check` exit 0. Open item for the owner: `references/patterns.md` §3 still lists `4` as the preferred focal-primary variant (locked from issue 18); the prototypes now deviate from it deliberately, and the matrix was **not** changed in this Gate.

### G4: Chart Guidance and Templates

- 2026-09-08: initialized as `planned`; no implementation evidence.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/references/charts.md` (185 lines): two-layer contract (library-neutral normative vs ECharts reference implementation), the ten-row selection matrix, the geographic-is-documentation-only section with the `registerMap` steps, the eight-row token bridge contract plus its five rules, the copy/adjust table (six pieces verbatim vs four must-adjust items), space/size/type/stroke rules, the four exception states, anti-patterns, performance guards, theme-switching rules, and the seven-template index. Directed: matrix data rows 10/10 and all ten locked data shapes present (time series / part-to-whole / ranking / distribution / density / geographic / relationship / single value / multidimensional / hierarchy); bridge rows 8/8 with every locked token name; six-piece copy table complete; must-adjust list complete (series data, axis categories, colour role, chart type itself); `npx eslint --no-cache references/charts.md` exit 0. Colour scan: exactly one literal in the file — `rgba(255,255,255,0.8)`, quoted as the ECharts `showLoading` default that §6 tells the reader to override; it is a documented library default, not a palette value, and `--dvk-color-*` remains the only colour source the file prescribes. Next: slice 2 — the first chart factory.

- 2026-09-09: slice 2 — wrote `skills/datav-kit/assets/charts/line-area.js` (366 lines). Exports `createLineArea(el, data, tokens = readDatavTokens(el))` and returns `{ chart, update, setState, setTheme, dispose }`. Directed six-piece inspection (scripted, 15 checks): token injection via `getComputedStyle` → `buildTheme()` → `echarts.init(el, buildTheme(tokens), { renderer: 'svg' })` PASS; explicit option skeleton (`grid: {left: 8, right: 12, top, bottom: 8, outerBoundsMode: 'same', outerBoundsContain: 'axisLabel'}`) PASS; `ResizeObserver` + `requestAnimationFrame` → `chart.resize()` PASS; `chart.setTheme()` PASS; four states present (loading with `maskColor: 'transparent'`, `graphic` empty, `role="alert"` failed overlay + `chart.clear()`, stale badge + dashed outline) PASS; performance guard (`sampling: 'lttb'`, `animationThreshold: 2000`, 4-line cap with a `console.warn`, min-size guard 160x100 → `dvk-count-to`/text degrade) PASS; no map/geoJSON/`registerMap` assumption PASS. `node --check` exit 0; `npx eslint --no-cache` exit 0 after one fix (the initial `parseGlow` regex tripped `regexp/no-unused-capturing-group` + `regexp/no-super-linear-backtracking`; replaced with a regex-free token split that also handles both computed `box-shadow` serialisations, colour-first and colour-last). Design decisions recorded: (a) `smooth: false` — design-rules 6.5 forbids over-smoothing, while prototype T2 uses `smooth: true`; charts.md is the chart authority, so the template follows the rule and T2 is left untouched (out of this Gate's scope, carried forward as a G3 artifact note). (b) `connectNulls: false` and `scale: true` on the value axis, so a line chart need not start at 0 (design-rules 6.4) and missing points break the line. (c) The template is self-contained (bridge + states + resize inlined in every module) because issue 10 locks "六件套内置/照抄" and the modules must be copy-and-run as single files. Next: slice 3 — `bar-rank.js`.

- 2026-09-09: slices 3-8 — wrote the remaining six factories from the same template, each in the Objective's file order, each verified on its own before the next: `bar-rank.js` (362 lines, `createBarRank`, `{ items: [{ name, value }], unit }`, horizontal sorted bars, `xAxis.min: 0`, direct right-edge value labels, `large: true` + `largeThreshold: 400`); `pie-doughnut.js` (363, `createPieDoughnut`, `{ items }`, `radius: ['58%','75%']`, `avoidLabelOverlap`, `minAngle: 6`, `percentPrecision: 2`, `showEmptyCircle: false` because datav-kit owns the empty state, 5-category cap); `scatter.js` (363, `createScatter`, `{ series: [{ name, points: [[x, y, size?]] }] }`, third element → `symbolSize`, `large: true` + `largeThreshold: 2000`); `gauge.js` (376, `createGauge`, `{ value, min, max, unit, label }`, one pointer, 210°→-30°, `progress` + `roundCap`, value animation gated by reduced motion); `radar.js` (365, `createRadar`, `{ indicators, series }`, `shape: 'polygon'`, 5-indicator cap, no splitArea); `heatmap.js` (368, `createHeatmap`, `{ xLabels, yLabels, cells: [[x, y, value]] }`, two category axes, `visualMap` with a single-hue alpha ramp — no rainbow, `progressive: 4000`). Per-slice directed evidence, all seven: `node --check` exit 0; `npx eslint --no-cache` exit 0; scripted 15-check six-piece inspection 15/15 PASS each (export, `getComputedStyle` token injection, `init(el, buildTheme(tokens), { renderer: 'svg' })`, explicit geometry — `grid` + `outerBoundsMode: 'same'` on the four cartesian types, explicit `center`/`radius` on pie/gauge/radar — `ResizeObserver` + rAF + `chart.resize()`, `chart.setTheme()`, four states, `animationThreshold: 2000` + a type-specific guard, and no map/geoJSON assumption). Aggregate: 7/7 modules pass, FAILURES 0. Structural identity check: the bridge region, the resize region, and the four-state region are **byte-identical across all seven files**; the factory body is identical modulo the export name and the one degrade-value expression — so "copy the six pieces verbatim" is literally true across templates. One fix during these slices: `scatter`'s `typeTheme` initially ignored its `t` parameter (`unused-imports/no-unused-vars`), fixed by giving the marks a token-derived rim. Next: repository verification (node --check loop, `pnpm lint`, `git diff --check`) plus a browser smoke.

- 2026-09-09: repository verification — `for f in skills/datav-kit/assets/charts/*.js; do node --check "$f"; done` exit 0 (7/7); `pnpm lint` exit 0; `npx eslint --no-cache skills/datav-kit/references/charts.md skills/datav-kit/assets/charts/` exit 0, and the JS modules are genuinely inside the antfu lint scope (unlike G3's `.html`), evidenced by real lint errors surfacing on these files during development (`regexp/no-unused-capturing-group`, `unused-imports/no-unused-vars`) before the fixes; `git diff --check` exit 0 with empty output, run via `git add -N` on the new files followed by `git reset` to restore the untracked working-tree state. Source Baseline: all 48 listed SHA-256 inputs re-verified byte-for-byte → 48/48 match. Working tree holds only `references/charts.md`, the seven modules, and this Progress Log; no unrelated file was touched.

- 2026-09-09: browser smoke (supplementary directed evidence) — Chrome 152.0.7977.76 headless via Playwright (`channel: 'chrome'`), harness served over `http://localhost` from `/tmp/dvk-smoke` (symlinks to the repo modules, `assets/tokens.css` plus the five `@datav-kit/themes@0.0.5` stylesheets and `echarts@6.1.0` ESM from jsDelivr through an import map), 1920x1080. Results: 7/7 factories mounted and rendered a real SVG at 456x240; **0 console errors / 0 warnings / 0 page errors / 0 failed requests**. Four states on a live instance: loading text `加载中` inside the SVG, empty text `暂无数据`, failed → `[role="alert"]` with `数据获取失败`, stale → `[role="status"]` badge plus the dashed outline, ready clears both. Theme: `--dvk-color-primary` cyber-blue `#18f0ff` → matrix-green `#32ff7e`; `chart.setTheme()` kept the same instance (no re-init) and the SVG paint set changed `#18f0ff`/`#2b7cff` → `#32ff7e`/`#00b86b`. Resize: container 456 → 360 px and `chart.getWidth()` followed through the ResizeObserver + rAF path. Min-size guard: a 120x80 container produced no chart and a value card reading `72`. Empty data after `update()` rendered `暂无数据`.

- 2026-09-09: two real defects found by the smoke run, fixed in the shared template, then all seven modules regenerated and re-verified (node --check, lint, 15-check inspection, smoke): (a) `empty` and `failed` call `chart.clear()`, which drops the option, so returning to `ready`/`stale` left a blank chart — fixed with a `cleared` flag that redraws `buildOption()` when the chart comes back; (b) `chart.setTheme()` issued after a `clear()` + merge-mode `setOption()` left the chart blank permanently (2 SVG nodes) — this is the ECharts caveat r2 §2.1 records ("calling `setOption` multiple times in merge mode is not supported when using `setTheme`"), fixed by rebuilding the option with `notMerge: true` after `setTheme`, which is the remedy r2 prescribes. After the fixes the smoke run reports `themePaintChanged: true` with non-empty before/after paint sets and the same 0-error console.

- 2026-09-09: Exit conditions — (1) PASS: `references/charts.md` holds the complete library-neutral matrix (10 data shapes), the 8-row token bridge, the anti-pattern section, and the copy/adjust rules. (2) PASS: all seven factories exist with the required export and the six-piece contract — 15/15 scripted checks per module, the §10 index matches the real exports 7/7, and the returned handle (`chart`/`update`/`setState`/`setTheme`/`dispose`) is byte-identical across all seven. (3) PASS: `node --check` loop exit 0, `pnpm lint` exit 0, targeted no-cache lint exit 0, `git diff --check` exit 0. (4) PASS: no map template (no `registerMap` / geoJSON / `type: 'map'`), no chart wrapper component (no `customElements.define`), and no second token or colour source — the only colour literals are the documented `TOKEN_FALLBACKS` defaults (the same values r2 records) and one quoted ECharts default mask inside a comment; every rendered colour derives from `--dvk-color-*` through `withAlpha()`. Manual acceptance: 无. Risks / carry-forward: (a) ECharts' internal helper `<path>` still paints its library default `#b7b9be` (the same non-rendered artefact G3 noted); no theme token can reach it. (b) `setTheme` now re-applies the option with `notMerge`, so a theme switch replays the entry animation and resets interaction state — the officially sanctioned tradeoff, irrelevant on an unattended screen. (c) The templates are deliberately self-contained (bridge, resize, and state blocks byte-identical across all seven; only the export name and the degrade-value expression differ), so copying one file runs on its own. (d) `smooth: false` in the line template versus `smooth: true` in prototype T2 — charts.md is the chart authority and design-rules 6.5 forbids over-smoothing; T2 is untouched as a G3 artefact.

- 2026-09-09: G4 `active` → `passed`; G5 `planned` → `active` — 已激活，尚未开始实施. Effort continues in a new Goal; this Goal ends here.

### G5: Project Theme and Contrast Utility

- 2026-09-08: initialized as `planned`; no implementation evidence.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/assets/themes/theme-template.css` (94 lines). Eight declarations under a single selector `.dvk-theme-<project>`: `--dvk-color-primary`, `--dvk-color-secondary`, `--dvk-color-accent`, `--dvk-color-surface`, `--dvk-glow-soft`, `--dvk-glow-strong`, `--dvk-line-width`, `--dvk-motion-duration`. Directed: declaration scan over the comment-stripped file → 8/8 exactly the locked set (0 missing, 0 extra, 0 non-`--dvk-*` custom properties), one selector, no `:root`; `--dvk-screen-*` declarations 0 (one comment mention deliberately names the layer boundary: "Never put a screen token in a theme"); no chart/prototype code (`echarts` / `customElements` / `dvk-border-box-*` / `dvk-count-*` absent). Guidance presence 16/16: the 8-row derivation table (brand colour → primary / secondary / accent / surface / glow / line-width / motion-duration), lighten-or-desaturate until AA is met and do not keep a failing brand colour, dark surface = lightness 5–10% + alpha 0.72 and never pure black, glow = primary + alpha 0.55 / 0.85 with blur radius 12 / 24px, line-width 1px, motion 2200–2600ms with the interaction-motion split, complete-declaration / no-partial-inheritance rule, coexist-with-the-library-themes rule (a project theme is a sixth complete theme class), one-theme-per-screen with `--dvk-color-accent` for emphasis instead of a second theme, the `design/theme.css` landing point, the checker entrypoint, and the single-colour-source rule. The worked example is traceable to the brand seed `#1f6fd0` and each value follows one of the table's own options: primary = seed lightened to hsl(209, 100%, 65%); secondary = primary lightness −20%; accent = complement (hue +180°) desaturated for the dark scene; surface = seed lightness 6% + alpha 0.72.

- 2026-09-09: slice 2 — wrote `skills/datav-kit/assets/tools/contrast-check.js` (261 lines). Exports `parseColor`, `relativeLuminance`, `contrastRatio`, `GROUPS`, `check`, `checkAll`, `formatReport`; runs its own four-group demo when executed directly. Four locked groups with floor and source label each: text 4.5:1 (design-rules 4.1 body text), graphic 3:1 (4.1 non-text), decorative-line 3:1 (4.1 non-text), adjacent-marks 3:1 (4.1 non-text applied to the advisory, conditional 4.8); the shared advisory band 7:1–15:1 (4.5) is reported as a note and never fails. Large text uses the same text group at its 4.1 floor of 3:1. Translucent colours are composited over an explicit `ground`, so `--dvk-color-surface` (alpha 0.72) needs the screen ground and a missing ground throws rather than silently changing the semantics. Directed (35-check suite, 35/35 PASS): the four groups exist with floors 4.5 / 3 / 3 / 3 and a source label each; the ratio is returned and compared raw — the boundary fixture `rgba(255, 255, 255, 0.90014)` on `#6e6e6e` is `4.495083617967971`, displays as `4.50` at two decimals, and still FAILs, proving nothing rounds before the comparison; below-band and above-band advisories are both reported and neither changes PASS; alpha compositing matches an independently computed value; hex 3/4/6/8, `rgb(r g b / a)`, percentage channels, and unknown-input rejection all behave; an unknown group throws; every import is a `node:` builtin (`node:process`, `node:url`) and the module runs from an isolated directory with no `node_modules` present; `toFixed` / `Math.round` / `toPrecision` appear nowhere. Demo run prints one passing and one failing pair per group with raw ratios: 4 pass / 5 fail.

- 2026-09-09: decisions and findings in this Gate (no Stop condition): (a) The adjacent-marks floor is design-rules 4.1's 3:1 applied to 4.8, which is `[advisory]` and conditional ("when a boundary itself carries meaning"), so its `floorSource` label and the module header state that a FAIL there means the two marks are not told apart by colour alone and is fixed by colour-role assignment or a non-colour cue (direct label / shape), not necessarily by changing the palette. (b) That group has a hard ceiling on a dark theme: with `--dvk-color-surface` at lightness 5–10% (effective luminance 0.0045 for the worked example), three marks that each clear 3:1 against the surface cannot also be mutually 3:1 apart — the third would need luminance ≥ 1.42 — so at most two marks can pass every pairwise check. The template's example therefore clears the text / graphic / decorative-line floors (primary 7.68:1, accent 10.92:1, secondary 4.39:1 = non-text floor only, so the template says to raise its lightness before using it for body text) while three adjacent-mark pairs report FAIL by construction; both files document this instead of weakening the palette. (c) `--dvk-text-strong` / `-muted` / `-faint` used by the G3 prototypes are declared nowhere in the repo (absent from `packages/themes`, `packages/elements`, and `assets/tokens.css`), so prototype text currently inherits its colour; the prototypes are G3 artefacts and the scope boundary forbids revising them here, so this is carried forward to G6 as a release-acceptance item.

- 2026-09-09: repository verification — `node --check skills/datav-kit/assets/tools/contrast-check.js` exit 0; `pnpm lint` exit 0; `git diff --check` exit 0 with empty output, run via `git add -N` on the two new files followed by `git reset` to restore the untracked working-tree state. Lint scope caveat, recorded rather than glossed: `eslint.config.js` changed on disk during this Goal (not by this Gate) to `ignores: ['.scratch/**', 'skills/**']`, so `skills/**` now sits outside the antfu scope and `pnpm lint` no longer covers this Gate's files — `npx eslint --no-cache skills/datav-kit/assets/tools/contrast-check.js` returns "File ignored because of a matching ignore pattern". Supplementary evidence in its place: `npx eslint --no-cache --no-ignore skills/datav-kit/assets/tools/contrast-check.js` exit 0, after fixing the three real findings it surfaced (`no-console` on the CLI output, `node/prefer-global/process` twice — resolved by writing to `process.stdout` and importing `process` from `node:process`); the CSS file has no eslint configuration (the same situation as `assets/tokens.css` in G0) and is covered by the directed checks. Source Baseline: 47/48 SHA-256 inputs match; the single mismatch is `eslint.config.js`, the user's own working-tree change, which this Gate did not touch. Working tree holds only the two new files plus that pre-existing config change.

- 2026-09-09: Exit conditions — (1) PASS: `assets/themes/theme-template.css` declares exactly the eight `--dvk-*` theme variables under `.dvk-theme-<project>` together with the derivation table, the dark-surface / no-pure-black rule, the complete-declaration / no-partial-inheritance rule, the coexistence rule, the one-theme-per-screen rule, and the `design/theme.css` landing point; no `--dvk-screen-*` declaration and no chart/prototype code. (2) PASS: `assets/tools/contrast-check.js` checks all four groups, returns raw ratios and compares them unrounded (the 4.495083617967971 boundary case FAILs while displaying as 4.50), reports the 7:1–15:1 advisory band without failing, and imports only `node:` builtins — it runs with no `node_modules` present. (3) PASS: `node --check` exit 0, `pnpm lint` exit 0, `git diff --check` exit 0, with passing and failing fixtures for every group in both the demo run and the 35-check directed suite. Manual acceptance: 无. Risks / carry-forward: (a) `pnpm lint` no longer covers `skills/**` after the config change above, so later Gates cannot use it as coverage evidence for skill files. (b) The adjacent-marks group's advisory/conditional semantics and its dark-surface ceiling are documented in both new files. (c) The undefined `--dvk-text-*` tokens in the G3 prototypes are carried to G6.
- 2026-09-09: G5 `active` → `passed`; G6 `planned` → `active` — 已激活，尚未开始实施. Effort continues in a new Goal; this Goal ends here.

### G6: Minimal Example and Release Acceptance

- 2026-09-08: initialized as `planned`; no implementation evidence.

- 2026-09-09: slice 1 — wrote `skills/datav-kit/assets/minimal-example.html`. One screen + three panels + one chart, and deliberately not a fifth prototype: no KPI strip, no map, no third column. Skeleton header `--dvk-screen-header-height` 104 / main `minmax(0, 1fr)`, 12 columns, gutter 24, margin 48, left span 8 = chart panel, right span 4 = two stacked panels. Blocks: P2+P3 title bar (feature-detected on `dvk-title-1/2/3`, P3 paired `dvk-decoration-9` rails as the published-0.0.5 fallback), P6+P7+chart slot (line-area, two series), P6+P7+P9 (three progress rows), P6+P7+P15 (two secondary metrics), P16 for every number. Real components `dvk-fit-screen` / `dvk-border-box-15` / `dvk-border-box-9` / `dvk-decoration-9` / `dvk-count-to`; one inline ECharts instance (SVG renderer) with the token bridge, explicit `grid` override, ResizeObserver+rAF resize, `setTheme` on theme switch (never re-init), chart animation 300ms, `smooth: false` (design-rules 6.5, `charts.md` is the chart authority). Colours only from `--dvk-color-*`, sizing only from `--dvk-screen-*`, exactly one `.dvk-theme-*` class, `prefers-reduced-motion` sets `disabled` on every `dvk-count-to`, and the header carries the 6.7 freshness line instead of a blank chart. Directed browser smoke (`file://`, Chrome 152 headless via Playwright, 1920x1080): **0 console errors / 0 warnings / 0 page errors / 0 failed requests**; chart renders a real 1156x770 SVG; title path `p3-fallback`; 5 `dvk-count-to`; one theme class; no box-level overflow; 0 `[slot]` attributes; theme switch cyber-blue → matrix-green flips `--dvk-color-primary` #18f0ff → #32ff7e and the SVG paint set (24,240,255 / 43,124,255 → 50,255,126 / 0,184,107). Screenshot inspected: header rails + centred h1, chart, progress bars and metric cards all render correctly. Risks / carry-forward: (a) ECharts' internal helper `<path>` keeps its library default `rgb(183,185,190)` — the same non-rendered artefact recorded in G3/G4. (b) The example deliberately carries no four-state demo; the four-state contract stays owned by `references/charts.md` and the seven chart templates.

- 2026-09-09: slice 2 — wrote `skills/datav-kit/README.md` (Chinese, human-facing). Contents: coverage declaration (published 30 / `main`-only 5 / `title-4` nonexistent) with the `customElements.get(tag)` runtime authority and the "from main — may not be published" label; the install command `npx skills add hackycy/datav-kit` plus the `--list` variants (branch ref and local path) and why the default-branch form can return nothing; the design-tradeoff summary (single colour source, `--dvk-screen-*` not in themes, one theme per screen, computed `contentRect` inset is the safe distance and inner wrappers carry extra padding, component-agnostic header via `customElements.get('dvk-title-1')`, default-slot titles, no component/chart wrapper, online-first knowledge base); the `assets/minimal-example.html` entrypoint and the T1–T4 table; the resident vs on-demand reading guide with a recommended reading order; and the six-item post-release checklist (new component / renamed props / theme token keys / prototypes still run / chart templates still run / empty directory became a real component). Directed: 12/12 README `./` links resolve, all 22 explicit spec §2 paths exist, and SKILL.md's on-demand index now resolves 6/6 — closing G2's carry-forward (a). `pnpm lint` does not cover `skills/**` (the `eslint.config.js` ignore recorded in G5), so coverage is: `npx eslint --no-cache skills/datav-kit/README.md` → "File ignored because no matching configuration was supplied" (0 errors / 1 warning, exit 0), and `npx eslint --no-cache --no-ignore skills/datav-kit/README.md` → exit 0 clean. The `.html` example has no eslint configuration, the same situation as the prototypes and `tokens.css`, and is covered by the directed browser checks.

- 2026-09-09: slice 3 — repository/CLI/browser verification set. **CLI (three variants, recorded rather than glossed)**: (a) `npx skills add hackycy/datav-kit --list` clones the default branch `main`, which does not carry the skill yet → "No skills found"; (b) `npx skills add hackycy/datav-kit#feat/datav-skills --list` → **Found 1 skill: `datav-kit`** with the locked `description`, proving the published remote tree is listable; (c) `npx skills add /path/to/datav-kit --list` → **Found 1 skill: `datav-kit`**, proving the local tree is listable. (a) is expected and is not a CLI failure: the skill lives on the pushed `feat/datav-skills` branch and merging to `main` is an owner action outside this Goal (no remote push in this effort). **Repository**: `pnpm lint` exit 0; `pnpm typecheck` exit 0; `pnpm test` exit 0 (8/8 tasks); `pnpm build` exit 0 (5/5 tasks); `git diff --check` exit 0 with empty output (run via `git add -N` on the two new files followed by `git reset`). **Browser**: all four prototypes plus the minimal example re-opened from `file://` after the tree was assembled — 5/5 PASS with 0 console errors / 0 warnings / 0 page errors / 0 failed requests, no cross-file path or CDN regression; T2's two chart slots and the example's chart slot all render real SVG, T1/T3/T4 declare no chart slot by their locked block lists. Source Baseline: 47/48 SHA-256 inputs match; the single mismatch is `eslint.config.js`, the user's own pre-existing working-tree change (recorded in G5), which this Gate did not touch.

- 2026-09-09: slice 3, Stop-condition finding and user-authorized exception — the first `pnpm test` run failed with 3 failures in `packages/elements/test/register.test.ts` (loading-energy). Diagnosis: the assertions date from `ec49f62` (2026-07-06, "add LoadingEnergy component"); the component was redesigned in `adcb282` ("redesign component with status panel and data lanes", 34 commits before this effort) without updating them, so the tests query parts that no longer exist (`module-shell`, `bus-line`, `bus-flow`, `energy-cell`, `energy-fill`, `energy-flow`, `scan-line`, `charge-segment`, `core`) and expect a `<path part="frame">` where the component now renders a `<rect part="frame">`. Ownership was cleanly separable: the working tree held only the two new untracked skill files, the failure reproduces with only `packages/elements`'s own test, no CI job runs tests, and G0–G5 never ran `pnpm test`. The plan's G6 Stop condition "Repository checks fail without a fix that stays inside this Gate's scope" was therefore in tension with the Scope boundary that forbids component/test changes; this was surfaced to the user, who authorized an explicit out-of-scope exception: **fix the stale test**. Action: rewrote only the three loading-energy tests to assert the redesigned component's real contract — the attribute mapping test now checks the `panel` / `frame` / `header-line` / `divider` / `status-dot` geometry and the three `data-lane` groups (marker / track / progress `x`, `y`, `width`, `rx`, `fill`, opacity) plus the five `<animate>` elements (`opacity`, `r`, then three `x` with staggered `begin` 0s / 0.22s / 0.44s) and 0 `animateTransform`; the paused test checks `frame` + 3 data lanes + 0 animations; the fallback test checks both the `--dvk-color-primary` / `-secondary` bridge (`#18f0ff` / `#2b7cff` → `rgba(43, 124, 255, 0.38)`, `rgba(24, 240, 255, 0.46)`) and the component's built-in fallback (`rgba(138, 153, 173, 0.38)`). Assertion density is preserved (the mapping test still asserts ~40 specifics), so nothing was deleted, skipped, or weakened. Result: `packages/elements` 102/102 pass, `pnpm test` exit 0. Observation carried to the handoff, not fixed: `dvk-loading-energy`'s built-in fallback is `#1677ff` / `#8a99ad`, while `dvk-loading-orbit` and the `dvk-decoration-*` family fall back to the datav-kit brand colours `#18f0ff` / `#2b7cff` — the test names still say "datav-kit colors", so the two loading components disagree about their own fallback. Files touched: `packages/elements/test/register.test.ts` (test only; no component code changed).

- 2026-09-09: slice 4 — wrote the two acceptance cases under `.scratch/datav-kit-skill/acceptance/`, each with the six workflow artifacts (`design/brief.md` seven answers, `design/prototype-*.html` ×2, `design/decision.md` selection + deviation register, `index.html` delivered screen, `design/self-check.md` 1:1 rubric, `design/review.md` review entrypoint + skill revision items). Neither case is an existing repository Demo. **Case A — 城市轨道交通线网运营指挥大屏** (`case-a-metro-command/`): 校准 (6000/200)×(1080/2250)=14.4→14px，默认字号组；候选 T1 三栏运维（选定）+ T3 单栏叙事；主题 cyber-blue；偏离 D1–D6（字阶 4 级、分类色 2、标记数、P10 名称列 150px、P12 柱槽 56px、柱状窄域轴 `min: 90`）。**Case B — 智慧园区能耗与碳排驾驶舱** (`case-b-energy-carbon/`): 校准 (5000/200)×(1080/2160)=12.5<14→默认 14px；候选 T4 KPI 主导（选定，6 张放大 KPI 卡 + 6/6 双面板）+ T2 两栏分析（两个图表槽）；主题 solar-gold；偏离 D1（P10 名称列 150px），其余建议值全在允许区间内并逐条记录。Directed evidence, all recorded in each case's `self-check.md`: hardcoded-value scan finds 0 unregistered literals (Case A: 19 px literals all classified — canvas constants / pattern values / scene values / registered deviations; Case B: 13, same classification); every redline item carries a PASS with evidence; the four exception states were exercised live on both delivered screens (loading / empty / failed `role="alert"` / stale keeps the last data + badge + dashed outline, back to ready clears all) with **0 console errors**; `decision.md` registers every advisory deviation with a one-line reason and leaves no unregistered deviation. Each `review.md` lists build-time skill revision items (Case A: R1–R6; Case B: R7–R10) and marks the reviewer verdict as 待填写 — **Leo's review is not fabricated**. Browser smoke over all 11 HTML artifacts (4 skill prototypes + minimal example + 4 case prototypes + 2 case implementations) from `file://`: 11/11 PASS, 0 console errors / 0 warnings / 0 page errors / 0 failed requests, no box overflow. Repository re-verification after slice 4: `pnpm lint` 0, `pnpm typecheck` 0, `pnpm test` 0 (102/102 in `packages/elements`), `pnpm build` 0, `git diff --check` 0 (the runbook's stray trailing blank line was removed). Defects found and fixed while building the cases: (a) P10 row caps — 4 rows overflow a 352px panel, capped at 3 (Case A) / 5 rows fit at 624px (Case B); (b) Case A T3's footer label overlapped the bar by 5px, fixed by `line-height: 1.2` + a 52px slot floor (the skill's own T3 touches by 1px — carried as revision item R5); (c) Case A's 98.6 KPI rounded to 99 — fixed with an explicit `decimals` field; (d) Case A's bar labels crowded the plot edge — `grid.right` 48 → 76; (e) panel gap aligned to design-rules 2.2's 16px, which exposed the skill prototypes' 24px as a deviation (revision item R2).

- 2026-09-09: **人工验收交接** — G6 的 Manual acceptance 不是「无」，本次 Goal 到此结束。以下为交接记录，**不是 `blocked`、不是等待循环、不是 Gate 状态转换**；G6 保持 `active`。

  **验收入口**
  - 案例目录：`.scratch/datav-kit-skill/acceptance/case-a-metro-command/` 与 `.scratch/datav-kit-skill/acceptance/case-b-energy-carbon/`
  - 评审文件：各自的 `design/review.md`（含评审依据、待填结论、skill 修订项）
  - 交付实现：各自的 `index.html`（双击可开，1920×1080）
  - 候选原型：各自的 `design/prototype-*.html`（双击可开）
  - 最小示例：`skills/datav-kit/assets/minimal-example.html`

  **自动化验证证据**
  - 浏览器（Chrome 152 headless via Playwright，`file://`，1920×1080）：11/11 文件 PASS，0 console error / 0 warning / 0 page error / 0 failed request，无 box 溢出；两个交付实现的四态（实时 / 加载中 / 无数据 / 失败 / 陈旧）逐态实测通过。
  - 自检：两份 `design/self-check.md` 的硬编码值扫描各 0 处未登记值；红线 9/9 + 一屏一套主题 10/10 PASS。
  - 仓库：`pnpm lint` 0、`pnpm typecheck` 0、`pnpm test` 0、`pnpm build` 0、`git diff --check` 0。
  - Skills CLI：`npx skills add hackycy/datav-kit#feat/datav-skills --list` → Found 1 skill `datav-kit`；`npx skills add <repo 路径> --list` → Found 1 skill `datav-kit`；默认分支 `main` 尚无该 skill（未 push，属 owner 动作）。
  - 未满足项：**Exit condition 4 的 `pnpm test` 一度为红**，已按用户授权修复陈旧测试后转绿；Exit condition 5 依赖本次人工结论，尚未满足。

  **最小验收清单**（逐条给结论，**不打总分**）
  - 红线（每案例 9 条 + 一屏一套主题）：1.1 等比缩放 · 1.2 内容安全区 · 1.3 无溢出裁切 · 3.1 最小字号 · 3.2 关键数据不依赖 hover · 4.1 对比度下限（不可四舍五入）· 4.2 无色相多义 · 5.1 装饰必须语义化 · 5.2 动效可控 · 一屏一套主题。
  - 建议值偏离（逐条确认已登记且理由成立）：Case A D1 字阶 4 级 / D2 分类色 2 / D3 标记数 / D4 P10 名称列 150px / D5 P12 柱槽 56px / D6 柱状窄域轴；Case B D1 P10 名称列 150px。
  - 交付实现与候选原型实际打开一次（控制台无报错、内容不裁切、主题正确）。

  **期望的明确回复格式**
  - 通过：`通过`
  - 未通过：`未通过：<case/path> <item> <evidence>`

  **下一动作**
  - 收到 `通过` 后，在新 Goal 中把 G6 的 Exit condition 5 记为人工证据，逐项验收并考虑将 G6 标记 `passed`（最后一个 Gate，随后记录 effort 完成）。
  - 收到 `未通过：…` 后，按证据修复或回原型重选（硬闸门 3 / 4），并把该条追加到对应 `design/review.md` 的 skill 修订项表。
  - 另有 4 项待办不阻塞验收：① `pnpm test` 的陈旧测试修复属用户授权的 Gate 外改动（仅测试文件，未动组件代码）；② 10 条构建期 skill 修订项（R1–R10）待 owner 排期；③ `dvk-loading-energy` 与 `dvk-loading-orbit` 的内置回退色不一致（观察项，未修）；④ 默认分支 `main` 尚未包含 skill，`npx skills add hackycy/datav-kit --list` 需先合并 `feat/datav-skills`。

- 2026-09-09: **人工验收结果（新 Goal 输入）** — `未通过：skills/datav-kit/assets/minimal-example.html 标题等元素摆放错乱`。验收意见同时要求示例本身必须美观。G6 保持 `active`，按硬闸门 3 修复后重新自检，不改变 Gate 状态。

- 2026-09-09: slice 5 (post-acceptance fix) — repaired `skills/datav-kit/assets/minimal-example.html` after the 未通过 verdict. **Root cause (measured, not guessed)**: `.screen-header` is a three-column grid (`minmax(0,1fr) auto minmax(0,1fr)`) but the file had dropped the P2 left/right info boxes and never re-placed `#title-group`, so the title group became the *first* grid item and landed in column 1 — measured at x=48 w=888 with the title block centred on x≈492 against a screen centre of 960. The freshness chip was `position: fixed` at `top: 24px`, i.e. floating above the canvas rather than inside the header band. Fixes: (a) restored P2's three-part header — left `SCOPE` box, centred `#title-group`, right `UPDATED` box — so the title group occupies the auto centre column; re-measured title group at x=600 w=720, **centred on x=960 = header centre**, with symmetric 128px rails; (b) the freshness line (design-rules 6.7) moved into the right box with its live dot, no more fixed positioning, and the copy reduced to the timestamp (`UPDATED / 14:32:05`) since the label already carries the meaning; (c) the P9 list had ~170px of dead space under three top-aligned rows — it now fills the body with `height: 100%` and centres its rows; (d) the single chart read as a solid mass at 1208x856, so the two series now cross (在线设备 peaks at 11, 活跃会话 at 12), the area fill dropped 0.16 → 0.10, the value axis gained `splitNumber: 6`, and the grid margins widened to 56/32/44/32. Verified: `file://` smoke **0 console errors / 0 warnings / 0 page errors / 0 failed requests**, chart renders a real 1156x770 SVG, title path `p3-fallback`, 5 `dvk-count-to`, one `.dvk-theme-*` class, no box overflow, 0 `[slot]` attributes; theme switch still flips tokens and chart paint; screenshots inspected at 1920x1080. Aggregate re-verification: 7/7 HTML targets PASS (4 skill prototypes + minimal example + 2 case implementations), `pnpm lint` 0, `pnpm typecheck` 0, `pnpm test` 0, `pnpm build` 0, `git diff --check` 0. Files touched: `skills/datav-kit/assets/minimal-example.html` only.

- 2026-09-09: **人工验收交接（第二次）** — 本轮修复完成后重新交接，G6 仍为 `active`，本次 Goal 到此结束。

  **验收入口**（在上一轮基础上更新）
  - 最小示例（本轮修复对象）：`skills/datav-kit/assets/minimal-example.html` —— 双击可开，标题居中、顶栏三段、更新态在右上角、进度行垂直居中、图表双序列交叉。
  - 两个案例目录与各自 `design/review.md` 不变：`.scratch/datav-kit-skill/acceptance/case-a-metro-command/`、`.scratch/datav-kit-skill/acceptance/case-b-energy-carbon/`。
  - 四套原型：`skills/datav-kit/assets/prototypes/`。

  **自动化验证证据（本轮）**
  - 浏览器（Chrome 152 headless，`file://`，1920×1080）：7/7 目标 PASS，0 console error / 0 warning / 0 page error / 0 failed request，无 box 溢出；最小示例标题中心 x=960 与屏幕中心一致（修复前 x≈492）。
  - 仓库：`pnpm lint` 0、`pnpm typecheck` 0、`pnpm test` 0、`pnpm build` 0、`git diff --check` 0。

  **最小验收清单（本轮新增项）**
  - 最小示例：顶栏三段布局（左 SCOPE / 中标题 / 右 UPDATED）· 标题水平居中 · 更新态在顶栏内且带实时点 · 进度行垂直居中 · 图表双序列可辨识 · 主题切换后仍正确 · 控制台无报错。
  - 上一轮清单（红线 9 条 + 一屏一套主题 + 各案例建议值偏离）仍然有效，未变。

  **期望的明确回复格式**：`通过` 或 `未通过：<case/path> <item> <evidence>`。

- 2026-09-09: **人工验收结果（新 Goal 输入）** — 项目负责人回复 `通过`。这是 G6 Exit condition 5 的人工证据，不是自动推断。逐项验收与最终验证证据如下。

- 2026-09-09: G6 Exit conditions 逐项验收 —
  - **(1) PASS** — `assets/minimal-example.html` 满足其内容/运行时契约：一屏 + 3 面板 + 1 图表，双击可开，5 分钟可见完整效果；`README.md` 为中文，含覆盖范围声明（已发布 30 / 仅 main 5 / `title-4` 不存在）、安装命令、设计取舍摘要、最小示例入口、常驻 vs 按需阅读指引、发版后六项 checklist。
  - **(2) PASS** — 显式 skill 树完整（spec §2 的 22 个路径全部存在，`find` 计数 22，无 `.DS_Store` 之外的冗余）；Skills CLI 能列出 `datav-kit`：`npx skills add hackycy/datav-kit#feat/datav-skills --list` → Found 1 skill，`npx skills add <repo 路径> --list` → Found 1 skill。**残留项（非 skill 缺陷，属 owner 发布动作）**：不带分支的 `npx skills add hackycy/datav-kit --list` 会克隆默认分支 `main`（`9ed67b2`，早于本 effort），返回 "No skills found"，需先合并 `feat/datav-skills` 才成立；本 Goal 明确禁止 push 远程分支，故未执行。
  - **(3) PASS** — 四套原型与最小示例从 `file://` 打开：本轮 7/7 目标（4 原型 + 最小示例 + 2 案例实现）0 console error / 0 warning / 0 page error / 0 failed request，无 box 溢出；T2 两个图表槽与最小示例图表槽渲染真实 SVG；主题切换翻转令牌与图表配色；`customElements.get` 标题回退路径生效。
  - **(4) PASS** — `pnpm lint` 0、`pnpm typecheck` 0、`pnpm test` 0（`packages/elements` 102/102）、`pnpm build` 0、`git diff --check` 0。
  - **(5) PASS** — 两个新写的真实大屏案例（`case-a-metro-command` 城市轨道交通线网运营指挥、`case-b-energy-carbon` 智慧园区能耗与碳排）各含六件套产物（brief / 2 个候选原型 / decision / index.html / self-check / review），自检从 design-rules 1:1 派生且能查出硬编码值（两案例各 0 处未登记值）；项目负责人已给出明确验收回复 `通过`。

- 2026-09-09: G6 最终验证证据汇总（本 Gate 全量） — 浏览器：7/7 目标 PASS（Chrome 152 headless，`file://`，1920×1080，0 控制台输出，无溢出；最小示例标题中心 x=960）。仓库：`pnpm lint` / `typecheck` / `test` / `build` / `git diff --check` 全 0。CLI：本地路径与已推分支均列出 `datav-kit`；默认分支待合并（见 Exit 2 残留项）。人工：`通过`。授权例外：`packages/elements/test/register.test.ts` 的三条 loading-energy 陈旧断言按用户授权修复（仅测试文件，断言密度保留）。待办（不阻塞 G6）：10 条构建期 skill 修订项 R1–R10、`dvk-loading-energy` 回退色不一致的观察项、默认分支合并发布。

- 2026-09-09: G6 `active` → `passed`；G6 是最后一个 Gate，无直接后继，**effort 已完成**。工作区留待提交：`skills/datav-kit/{README.md,assets/minimal-example.html}`（新增）、`packages/elements/test/register.test.ts`（授权修复）、`.scratch/datav-kit-skill/acceptance/**`（验收证据）、本账本。本次 Goal 结束。
