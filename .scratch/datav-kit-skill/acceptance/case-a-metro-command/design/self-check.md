# self-check — 城市轨道交通线网运营指挥大屏

> 步骤 ⑤ 产物。清单从 `references/design-rules.md` **1:1 派生**（红线二值 / 建议值三档），不另写一套。
> 被判对象：`index.html`（交付实现）。跑法：实现完成后一次；此后每次改动重跑。
> 判定：`[redline]` = PASS / FAIL，任一条 FAIL 即不合格；`[advisory]` = 符合 / 已登记偏离 / 未登记偏离。

## 0. 硬编码值检查（必须先跑）

对 `index.html` 的 `<style>` 做 px 字面量扫描，逐个判定「令牌引用」还是「模式值 / 场景值 / 画布常量」。

```
px literals found: 19
```

| 字面量 | 判定 | 依据 |
| --- | --- | --- |
| `1920px` / `1080px` | 画布常量 | P1 设计画布，锁定值 |
| `4px solid` ×2（P2 左右信息块） | 模式值 | P2 参数：4px 强调左边框 |
| `min-width: 720px`（P3 标题轨） | 场景值 | P3 标题轨宽度是场景值 |
| `height: 60px`（P3 装饰轨） | 模式值 | P3 参数：轨高 58–64px |
| `220px`（T1 中部节奏行） | 场景值 | P12 栅格位置：中部底部行 |
| `5px 9px`（P8 状态片） | 模式值 | P8 参数：chip padding 5px 9px |
| `150px`（P10 名称列） | 已登记偏离 D4 | 120px 会截断站名 |
| `8px`（P9/P10 进度槽） | 模式值 | P9 参数：track height 8px |
| `56px`（P12 柱槽下限） | 已登记偏离 D5 | 流式行高给出的实际槽高远大于此 |
| `42px / 52px`（P11 徽标 / 时间列） | 模式值 | P11 参数：`42px minmax(0,1fr) 52px` |
| `3px solid`（P11 左边框） | 模式值 | P11 参数：`border-left: 3px` |
| `128px × 128px`（P13 环形） | 模式值 | P13 参数：近方形宿主 128×128 |
| `16px / 8px / 12px / 4px / 10px`（演示态控件） | 画布外 | `.demo-controls` 在 1920×1080 画布之外，不参与屏幕布局 |

结论：**0 处未登记硬编码值**。所有屏幕尺寸要么是 `--dvk-screen-*` 令牌引用（65 处），要么是上面逐条列出的模式值 / 场景值 / 画布常量；所有颜色都来自 `--dvk-color-*`（21 处引用），无颜色字面量。

## 1. 画布与栅格

| # | 档位 | 判定 | 证据 |
| --- | --- | --- | --- |
| 1.1 | `[redline]` | PASS | `dvk-fit-screen mode="contain" align="center center"`，无 `fill`，无响应式断点。 |
| 1.2 | `[redline]` | PASS | 全部面板内容在默认 slot 内，标题在 `.panel-inner`，未覆盖装饰线；`[slot]` 属性数 0。 |
| 1.3 | `[redline]` | PASS | 浏览器实测 `.screen / .screen-main / .col / .panel-inner / .panel-body / .chart-slot` 均无 `scrollWidth/scrollHeight` 溢出（0 条）。 |
| 1.4 | `[advisory]` | 符合 | 外边距 `--dvk-screen-safe-margin` = 48；单屏输出，无拼接缝，不启用 5% 加密。 |
| 1.5 | `[advisory]` | 符合 | 12 列 / 沟槽 `--dvk-screen-grid-gutter` 24 / 外边距 48。 |
| 1.6 | `[advisory]` | 符合 | 主区行 `minmax(0, 1fr)`，列内面板行 `repeat(2, minmax(0,1fr))`。 |
| 1.7 | `[advisory]` | 符合 | 缩放场景下细线用 1px 边框 + 组件 SVG，未依赖 1px CSS 边框做视觉分隔（分隔用 `--dvk-rule`）。 |
| 1.8 | `[advisory]` | 符合 | 1920×1080 `contain` + 居中；画布外留白由 `--dvk-page-ground` 氛围层填充。 |
| 1.9 | `[advisory]` | 符合 | 目标屏为 4m×2.25m LED，16:9 内容居中；非超宽 / 非竖屏，无降级分支。 |

## 2. 间距与节奏

| # | 档位 | 判定 | 证据 |
| --- | --- | --- | --- |
| 2.1 | `[advisory]` | 符合 | 全部间距取自 8pt 刻度令牌，无 40 之类非刻度值。 |
| 2.2 | `[advisory]` | 符合 | 外边距 48 / 区块间距 24 / 面板间距 16（`.col` gap = `--dvk-screen-space-lg`）/ 面板内距由组件 `contentRect` + 内层 16px / 顶栏 104 / KPI 104。 |
| 2.3 | `[advisory]` | 符合 | 7 个模块（KPI 条 1 + 线路负荷 + 站点客流 + 主视图 + 节奏 + 故障处置 + 设备可用率），落在 5–9。 |
| 2.4 | `[advisory]` | 符合 | 相邻区块间距 24 > 区块内最小间距 8。 |

## 3. 字体与层级

| # | 档位 | 判定 | 证据 |
| --- | --- | --- | --- |
| 3.1 | `[redline]` 🅰 | PASS | 校准 = (6000/200)×(1080/2250) = 14.4px → 最小字号 14px = `-size-xs`；页面无小于 14px 的文字。 |
| 3.2 | `[redline]` 🅰 | PASS | 全部核心指标静态可见，无 hover 依赖。 |
| 3.3 | `[advisory]` | 符合 | 1 个字体族（`--dvk-screen-font-family`）。 |
| 3.4 | `[advisory]` | 已登记偏离 D1 | 4 级字阶。 |
| 3.5 | `[advisory]` | 符合 | 字重 400 / 500 / 600 三档。 |
| 3.6 | `[advisory]` 🅰 | 符合 | 最长文本行为面板标题与告警描述，均 < 40 CJK；无两端对齐。 |
| 3.7 | `[advisory]` 🅰 | 符合 | 行高 `--dvk-screen-line-height-base` 1.5；紧凑标签单独设 1.2（仅单行标签）。 |

## 4. 颜色角色

| # | 档位 | 判定 | 证据 |
| --- | --- | --- | --- |
| 4.1 | `[redline]` 🅰 | PASS | 颜色全部来自 `cyber-blue` 主题的 `--dvk-color-*`；正文/标签用主色的 64% 混合（>4.5:1），非文字元素 ≥3:1；`contrast-check.js` 未用于本案例（本案例不生成自定义主题）。 |
| 4.2 | `[redline]` | PASS | 主色只表示「正常 / 主数据」，强调色只表示「告警 / 状态词」，无一个色相承担多种语义。 |
| 4.3 | `[advisory]` | 符合 | 角色账本 10 项中用到 6 项：中性背景 / 主焦点 / 次对比 / 正负变化 / 告警 / 选中。 |
| 4.4 | `[advisory]` | 已登记偏离 D2 | 分类色 2 / 强调色 2，未超上限。 |
| 4.5 | `[advisory]` | 符合 | 正文对比落在 7:1–15:1 区间；暗场景无纯白文字压纯黑。 |
| 4.6 | `[advisory]` | 符合 | 无连续 / 发散色阶，无彩虹。 |
| 4.7 | `[advisory]` | 符合 | 白色面积 0（暗色主题，无白色填充块）。 |
| 4.8 | `[advisory]` | 符合 | 相邻数据标记（柱 / 进度条）由位置而非仅颜色区分，且已直接标注数值。 |

## 5. 装饰与动效预算

| # | 档位 | 判定 | 证据 |
| --- | --- | --- | --- |
| 5.1 | `[redline]` | PASS | 装饰只有 P3 标题轨（映射到「屏标题」）与 P13 环形（映射到「设备可用率」）；无纯装饰光效。 |
| 5.2 | `[redline]` 🅰 | PASS | 无 >5s 自动播放；无闪烁；`prefers-reduced-motion` 下 `dvk-count-to` 置 `disabled`、图表 `animation: false`。 |
| 5.3 | `[advisory]` | 符合 | 交互动效无（只读）；装饰动效沿用主题 `--dvk-motion-duration`；图表动效 300ms（200–400ms 区间）。 |
| 5.4 | `[advisory]` | 符合 | 每区块 ≤1 装饰容器 + 1 装饰轨；装饰视觉权重低于同区块数据层。 |
| 5.5 | `[advisory]` | 符合 | 无大笔触、飘带、光斑、电影感壁纸、单色戏剧、装饰渐变。 |
| 5.6 | `[advisory]` | 符合 | 无渐变替代色阶、无光晕替代阴影。 |

## 6. 数据呈现与异常态

| # | 档位 | 判定 | 证据 |
| --- | --- | --- | --- |
| 6.1 | `[advisory]` | 符合 | 直接标注优先：柱状图右侧直标数值，无独立图例（图例仅折线场景）。 |
| 6.2 | `[advisory]` | 已登记偏离 D3 | 折线 0 / 饼图 0 / 仪表指针 1，未超限。 |
| 6.3 | `[advisory]` | 符合 | 无图例或图例高度远小于图表高 30%。 |
| 6.4 | `[advisory]` | 已登记偏离 D6 | 柱状数值轴未从 0 起（`xAxis.min: 90`）：准点率集中在 96–99.2%，从 0 起 6 根柱几乎等高；改用直接标注数值 + 窄域轴。 |
| 6.5 | `[advisory]` | 符合 | 无 3D 饼柱、无双轴、无彩虹、无过度平滑（`smooth: false`）。 |
| 6.6 | `[advisory]` | 符合 | 四态齐备并由画布外演示控件驱动：`实时 / 加载中 / 无数据 / 失败 / 陈旧`；失败态 `role="alert"`，其余 `role="status"`。 |
| 6.7 | `[advisory]` | 符合 | 陈旧态保留最后数据（图表 33 个 SVG 节点不变）+ 右上角「陈旧 · 14:32:05」+ 虚线轮廓，不出现空图。 |

## 跨组：一屏一套主题

| 条目 | 判定 | 证据 |
| --- | --- | --- |
| 一屏一套主题 `[custom]` | PASS | 屏幕根只有一个 `.dvk-theme-cyber-blue`，无第二套主题类。 |

## 结论

- **红线项：9/9 PASS，加「一屏一套主题」共 10 条二值判定全部 PASS**，无 FAIL → 可进入评审（硬闸门 3）。
- 建议值：28 条符合，4 条已登记偏离（D1 字阶 / D2 色数 / D3 标记数 / D6 柱状轴起点），0 条未登记偏离。
- 另有 2 条模式参数偏离（D4 P10 名称列 150px / D5 P12 柱槽下限 56px）已在 `decision.md` 登记。
- 硬编码值检查：0 处未登记硬编码值。
- 重跑触发：实现改动后必须重跑本清单。
